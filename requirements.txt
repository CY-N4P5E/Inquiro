# Core dependencies
langchain>=0.1.0
langchain-community>=0.0.10
langchain-text-splitters>=0.0.1
langchain-ollama>=0.0.1

# TUI Interface
textual>=0.41.0

# Vector storage (FAISS for local use)
faiss-cpu>=1.7.4

# Document processing (PyMuPDF for PDF and Unstructured for DOCX handling)
pymupdf>=1.23.0
unstructured>=0.10.0
python-docx>=0.8.11

# Local model support (Ollama)
# Note: Ollama needs to be installed separately from https://ollama.ai

# Testing
pytest>=7.4.0
